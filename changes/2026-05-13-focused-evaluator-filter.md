# Focused Evaluator Filter

Added `-OnlyRestoredNames` to `scripts/evaluate-recognition.ps1` so the corrected analysis can be evaluated on a named subset.

Focused subset for this pass:

- `Curse tablet`
- `Engraved ring of kinship`
