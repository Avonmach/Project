# Removed Unused Shape Comparison Payload

Cleaned up the remaining unused `shapeComparison` detection payload after removing the visible shape-vs-reference graphic.

Also centered the headers for the image/quantity/reference columns.

Verification:

- `node --check app.js`
