# Responsive layout and scroll

- Switched the workspace to one column at narrower widths before the right plan panel can squeeze the main table.
- Added internal horizontal scrolling for result tables and the restoration plan table so content stays inside panels.
- Preserved page scroll position around result rerenders to avoid jumping to the top after row/control actions.
