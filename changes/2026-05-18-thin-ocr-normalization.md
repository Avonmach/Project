# Thin OCR normalization

- Fixed OCR digit normalization to use non-overlapping pixel ranges.
- Added a regression test so a one-pixel source stroke stays one cell wide in the normalized detected grid.
