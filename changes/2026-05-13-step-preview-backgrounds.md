# Step Preview Backgrounds

Set the debug shape preview backgrounds to white so the black binary masks are visible.

Affected columns:

- `BG removed`
- `Cropped shape`
- `Scaled shape`

Verification:

- `node --check app.js`
- local server check at `http://127.0.0.1:8080`
