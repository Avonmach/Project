# OCR reference grids

- Added visible detected digit grids to the quantity OCR debug view.
- Added labeled reference digit grids for OCR alternatives so detected and reference shapes can be compared directly.
