# Create Analysis Export File

- Added an application helper that returns both export filename and payload.
- Reduced `main.ts` export handling to gathering runtime data and calling the browser download helper.
