# Modern UI and compact image headers

- Renamed cramped table image headers from `Screenshot`, `Reference`, and `Augmented` to `Crop`, `Ref`, and `Match`.
- Refreshed the interface with cleaner panels, softer borders, modern rounded controls, and updated table colours.
- Increased image column space slightly so headers no longer overlap.
