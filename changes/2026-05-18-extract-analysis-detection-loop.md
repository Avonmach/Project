# Extract Analysis Detection Loop

- Added a generic application use case for converting detected boxes into detection records.
- Kept browser-specific preview creation injected from `main.ts`.
- Removed quantity detection and record construction from the browser coordinator.
