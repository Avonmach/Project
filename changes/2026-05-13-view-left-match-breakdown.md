# View Left And Match Breakdown

Updated the results UI:

- Moved the `View` control to the left side of the results toolbar.
- Changed final score label from `match` to `Match`.
- Added separate score display for:
  - `Overlap`
  - `Color`
  - `Quantity`

Verification:

- `node --check app.js`
