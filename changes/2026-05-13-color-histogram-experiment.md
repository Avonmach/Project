# Color Histogram Experiment

Request: test recognition by analyzing only colors in the image after removing the bank background color, instead of directly matching the full image shape.

Planned evaluation: add foreground color histogram scoring to the offline evaluator and compare artefact accuracy on `Corrected_Analysis.json`.
