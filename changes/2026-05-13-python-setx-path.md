# Python PATH Persisted

Persisted the user PATH update using `setx Path ...`.

Installed executable verified directly:

- `C:\Users\avonm\AppData\Local\Programs\Python\Python312\python.exe --version` -> `Python 3.12.10`
- `python.exe -m pip --version` -> `pip 25.0.1`

Note: already-open terminals may still use the old PATH. Open a new terminal before using `python` directly.
