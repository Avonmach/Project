# Shape Comparison Server Check

Checked whether the local app is reachable at:

- `http://127.0.0.1:8080`

Use the page, reload, and click `Analyze` to see the new first-three shape comparison strip above the table.
