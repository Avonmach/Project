# Correct zero reference

- Replaced the OCR `0` template with the 8x5 bitmap normalized from `Testing/Image_Screenshot.png`.
- Skipped rendering `0` from `runescape-small-07` because the font-pack zero shape is wrong for the screenshot OCR.
- Added `Testing/1_New_Ref.png` as a visual reference for the corrected zero bitmap.
