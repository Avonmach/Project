# Python Path Check

Python installation was found at:

- `C:\Users\avonm\AppData\Local\Programs\Python\Python312\python.exe`

Current `python` command still resolves to the Windows app alias:

- `C:\Users\avonm\AppData\Local\Microsoft\WindowsApps\python.exe`

Next step: verify the real executable directly and update user PATH so it resolves first.
