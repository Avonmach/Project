# Extract Screenshot Analysis Frame

- Added an image-processing helper for preparing the canvas image data, bank grid, item boxes, and shape mask.
- Reduced `analyzeCurrentImage` to selecting mode, analyzing detections, rendering, and drawing overlays.
