# Python Install Check

Commands run:

- `python --version`: failed; `python.exe` exists but could not execute in this shell session.
- `py --version`: failed; Python launcher is not on PATH.

Next step: check whether `winget` is available for installation.
