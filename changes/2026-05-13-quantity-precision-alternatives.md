# Quantity precision and alternatives

- Added quantity OCR alternatives by combining the best per-digit candidates into full quantity candidates.
- Displayed the detected quantity precision and top alternative quantities under each quantity input for troubleshooting.
- Exported quantity alternatives alongside quantity confidence and quantity correction labels.
- Verified `app.js` with `node --check app.js` and confirmed the live server is serving the updated script.
